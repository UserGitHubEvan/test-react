import React from "react";
import footer from './Footer.module.css';

const Footer = () => {
    return (
            <footer className={footer.footer}>Hello footer!</footer>
    );
}

export default Footer;